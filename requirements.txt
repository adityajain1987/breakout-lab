# Breakout Lab — Python dependencies
# Pinned to match momentum-dashboard so OHLCV cache is interchangeable

# Data fetching
yfinance==1.3.0           # OHLCV from Yahoo Finance (.NS suffix for NSE tickers)
curl_cffi==0.15.0         # browser impersonation — bypasses Yahoo bot detection (required by yfinance 1.x)
requests==2.32.3          # HTTP for Bhavcopy + NSE bulk/block deals API
urllib3==2.2.3            # required by requests

# Data manipulation
pandas==2.2.3             # DataFrame core
numpy==2.1.3              # numerical
pyarrow==18.0.0           # parquet I/O for OHLCV cache

# Plotting + reports
matplotlib==3.9.2         # equity curve PNGs + volume profile previews

# UI (Phase 4)
streamlit==1.40.1         # dashboard MVP
plotly==6.7.0             # interactive candlestick + horizontal-bar volume profile

# Quality
pytest==8.3.3             # unit tests for analytics modules

# Notebooks (optional — exploration only)
jupyterlab==4.3.1         # ad-hoc analysis
