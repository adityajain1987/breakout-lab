# Breakout Lab — runtime dependencies for public deployment (Streamlit Cloud)
# Dev-only deps (pytest, jupyterlab) live in requirements-dev.txt and are NOT installed here.

# Data fetching — yfinance 0.2.x avoids the curl_cffi native-build hang on Streamlit Cloud
yfinance>=0.2.50,<1.0
requests>=2.32.0
urllib3>=2.2.0

# Data manipulation
pandas>=2.2.0,<3.0
numpy>=2.0.0,<3.0
pyarrow>=18.0.0

# Plotting
matplotlib>=3.9.0
plotly>=6.0.0

# Streamlit
streamlit>=1.40.0
