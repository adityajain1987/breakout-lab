# Dev-only dependencies (testing + notebooks).
# Install locally with:  pip install -r requirements-dev.txt
# NOT installed on Streamlit Cloud — that uses requirements.txt only.

-r requirements.txt

pytest>=8.3.0
jupyterlab>=4.3.0
